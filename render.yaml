services:
  - type: web
    name: ai-chat-site
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    region: singapore
    envVars:
      - key: OPENAI_API_KEY
        sync: false
